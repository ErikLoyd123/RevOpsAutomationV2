# =============================================================================
# RevOps Automation Platform - Environment Configuration
# =============================================================================
# Copy this file to .env and customize for your local development environment
# 
# Usage:
#   cp .env.example .env
#   # Edit .env with your specific configuration
#   docker-compose --profile gpu up
# =============================================================================

# =============================================================================
# Core Environment Configuration
# =============================================================================
ENVIRONMENT=development
LOG_LEVEL=info

# =============================================================================
# Database Configuration
# =============================================================================
# PostgreSQL with pgvector
POSTGRES_PASSWORD=revops_dev_password_change_me
POSTGRES_PORT=5432

# PgAdmin (Development only)
PGADMIN_EMAIL=admin@revops.local
PGADMIN_PASSWORD=admin_change_me
PGADMIN_PORT=5050

# =============================================================================
# Message Queue Configuration
# =============================================================================
# Redis for message queues and caching
REDIS_PORT=6379
REDIS_COMMANDER_PORT=8081

# =============================================================================
# BGE Embeddings Service Configuration
# =============================================================================
# Service Port
BGE_PORT=8007

# BGE Model Configuration
BGE_BATCH_SIZE=32                    # GPU: 32, CPU: 8
BGE_MAX_SEQUENCE=8192               # GPU: 8192, CPU: 4096
BGE_SIMILARITY_THRESHOLD=0.7
BGE_CACHE_SIZE=1000                 # GPU: 1000, CPU: 500

# =============================================================================
# Microservices Ports Configuration
# =============================================================================
# API Gateway
API_GATEWAY_PORT=8000

# Billing Normalization Service
BILLING_PORT=8010

# Opportunity Matching Service
MATCHING_PORT=8008

# POD Rules Engine Service
RULES_PORT=8009

# =============================================================================
# GPU Configuration (NVIDIA RTX 3070 Ti)
# =============================================================================
# These are automatically configured in docker-compose.yml
# Documented here for reference:
#
# NVIDIA_VISIBLE_DEVICES=0
# NVIDIA_DRIVER_CAPABILITIES=compute,utility
# CUDA_VISIBLE_DEVICES=0
# TORCH_CUDA_ARCH_LIST=8.6
# PYTORCH_CUDA_ALLOC_CONF=max_split_size_mb:128

# =============================================================================
# External Database Connections (Production Systems)
# =============================================================================
# These are used for data ingestion from production systems
# DO NOT use these in Docker containers - they are for ingestion scripts only

# Odoo Production Database (Read-only)
ODOO_DB_HOST=c303-prod-aurora.cluster-cqhl8dhxcebr.us-east-1.rds.amazonaws.com
ODOO_DB_NAME=c303_odoo_prod_01
ODOO_DB_USER=superset_db_readonly
ODOO_DB_PASSWORD=your_odoo_password_here

# APN/ACE Production Database (Read-only)
APN_DB_HOST=c303-prod-aurora.cluster-cqhl8dhxcebr.us-east-1.rds.amazonaws.com
APN_DB_NAME=c303_prod_apn_01
APN_DB_USER=superset_readonly
APN_DB_PASSWORD=your_apn_password_here

# =============================================================================
# Development Tips
# =============================================================================
# 
# Quick Start Commands:
# 
# 1. Start basic development stack (CPU-only):
#    docker-compose --profile dev up
# 
# 2. Start with GPU support:
#    docker-compose --profile gpu up
# 
# 3. Start with monitoring tools:
#    docker-compose --profile dev --profile monitoring up
# 
# 4. Start full stack:
#    docker-compose --profile full --profile gpu up
# 
# 5. Production deployment:
#    docker-compose --profile prod up -d
# 
# Service URLs (when running):
# - API Gateway: http://localhost:8000
# - BGE Embeddings: http://localhost:8007
# - Billing Service: http://localhost:8010
# - Matching Service: http://localhost:8008
# - Rules Service: http://localhost:8009
# - PgAdmin: http://localhost:5050
# - Redis Commander: http://localhost:8081
# 
# =============================================================================